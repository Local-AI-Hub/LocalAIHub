# Local AI Hub vX.Y.Z

Local AI Hub X.Y.Z [brief, factual summary of this release].

## Highlights

- [Most important user-visible change]
- [Second important change]
- [Important limitation or compatibility note, when relevant]

## New Features

- [Feature and the user problem it addresses]

## Fixes

- [Fixed behavior and its user-visible impact]

## Validation

The following documentation-safe or offline checks were run for this release:

- `npm run build:ui`
- `npm run build`
- [Add the exact offline verification commands that passed]

The following checks were not run:

- [List live provider, hardware-specific, or other manual validation that was intentionally skipped]

Do not describe a check as passing unless it completed successfully for this release.

## Known Untested Features

- [List features that remain untested on real hardware, with local model assets, or against live provider accounts]
- [Link to the README untested-features section when it remains the source of truth]

## Download

1. Open the Assets section below.
2. Download `LocalAIHub-Setup-X.Y.Z.exe`.
3. Run the installer and follow the Windows instructions.

The `.exe` is the installer. The `.blockmap` and `latest.yml` files support the application's release/update flow and normally should not be downloaded or opened manually.

## Windows SmartScreen and Unsigned Installer

Local AI Hub is currently distributed as an unsigned Windows app. Windows SmartScreen, browsers, or antivirus software may warn before the installer runs.

Download the installer only from the official Local AI Hub GitHub release. If SmartScreen appears, choose `More info`, verify that the file is the release asset you intended to download, and select `Run anyway` only if you trust the source and want to continue.

## Release Assets

- `LocalAIHub-Setup-X.Y.Z.exe`
- `LocalAIHub-Setup-X.Y.Z.exe.blockmap`
- `latest.yml`

Replace every `X.Y.Z` placeholder and remove unused bullets before publishing.
